[project]
name = "finnish-inflection-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "inquirerpy>=0.3.4",
    "rich>=14.3.3",
    "uralicnlp>=2.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
